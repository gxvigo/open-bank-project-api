swagger: '2.0'
info:
  x-ibm-name: test
  title: test
  version: 1.0.0
schemes:
  - https
host: $(catalog.host)
basePath: /test
consumes:
  - application/json
produces:
  - application/json
securityDefinitions:
  clientIdHeader:
    type: apiKey
    in: header
    name: X-IBM-Client-Id
security:
  - clientIdHeader: []
x-ibm-configuration:
  testable: true
  enforced: true
  cors:
    enabled: true
  assembly:
    execute:
      - gatewayscript:
          title: gatewayscript
          version: 1.0.0
          source: |-
            console.warn('### test');
            console.debug('### test');
    catch: []
  gateway: datapower-gateway
paths:
  /path-1:
    get:
      responses:
        '200':
          description: 200 OK
definitions: {}
tags: []
